<% include partials/header %>



    <h1>Home</h1>

    <p>
        <%= content %>
    </p>

    <h1>Posts</h1>

    <% postArray.forEach(i=> { %>
        <h2>
            <%= i.title %>
        </h2>
        <p>
           
            <%= i.content.substring(1,100) + "..." %> <a href="/post/<%= i._id %> ">Read more</a>
        </p>

        <%= i.day %>

            <% });%>




<% include partials/footer %>